# OpenClash 通用覆写：大陆直连 + 手工直连 + 其余美国手选
#
# 设计目标：
# 1. 中国大陆域名/IP默认直连。
# 2. rules/manual-direct.yaml 用于人工追加必须直连的域名。
# 3. 其余所有流量统一进入“美国”策略组。
# 4. “美国”为 select 手动选择组，仅展示订阅中的美国节点。
# 5. 非美国节点仍保留在订阅底层，但不进入任何策略组，也不会被规则使用。
# 6. 不按设备/IP/人员拆分规则，因此可供多人、多设备直接复用。
# 7. 不整体覆盖 rule-providers，避免删除 OpenClash 自动生成的 oc-cn-domain 等内部 provider。
# 8. 保留 Fake-IP，同时对局域网和自建回流域名返回真实 IP，降低 NAT 回流/内网访问异常概率。
#
# 适用：OpenClash Meta/Mihomo。
# 维护说明见仓库 README.md。

[General]
CORE_TYPE = Meta
PROXY_MODE = rule
RESTART = true
ENABLE_REDIRECT_DNS = 1
ENABLE_CUSTOM_DNS = 1
APPEND_DEFAULT_DNS = 0
APPEND_WAN_DNS = 0
ENABLE_RESPECT_RULES = 1
ROUTER_SELF_PROXY = 1
IPV6_DNS = 0
ENABLE_UDP_PROXY = 1
DISABLE_UDP_QUIC = 1
ENABLE_META_SNIFFER = 1
ENABLE_META_SNIFFER_PURE_IP = 1

[YAML]

profile:
  store-selected: true
  store-fake-ip: true

dns:
  enable: true
  ipv6: false
  respect-rules: true
  prefer-h3: false

  default-nameserver:
    - 223.5.5.5
    - 1.12.12.12

  proxy-server-nameserver:
    - https://223.5.5.5/dns-query
    - https://1.12.12.12/dns-query

  nameserver:
    - 'https://1.1.1.1/dns-query#美国'
    - 'https://8.8.8.8/dns-query#美国'

  direct-nameserver:
    - https://223.5.5.5/dns-query
    - https://1.12.12.12/dns-query
  direct-nameserver-follow-policy: true

  nameserver-policy:
    'geosite:cn,private':
      - https://223.5.5.5/dns-query
      - https://1.12.12.12/dns-query

  fake-ip-filter+:
    - '+.lan'
    - '+.local'
    - '+.home.arpa'
    - 'localhost'
    - 'pve.yangnas.cn'
    - 'auth.yangnas.cn'
    - 'nexus.yangnas.cn'

# ============================================================
# 海外策略组
# ============================================================
proxy-groups!:
  - name: 美国
    type: select
    include-all: true
    filter: '(?i)(🇺🇸|美国|美國|United[ _-]?States|USA|America|Los[ _-]?Angeles|San[ _-]?Jose|Seattle|New[ _-]?York|Chicago|Dallas|(?:^|[ _-])US(?:$|[ _-]|[0-9]))'

# ============================================================
# 手工直连规则提供者
# ============================================================
rule-providers:
  Manual-Direct:
    type: http
    behavior: classical
    format: yaml
    proxy: DIRECT
    url: https://raw.githubusercontent.com/yang137197/openclash-custom-rules/main/rules/manual-direct.yaml
    path: ./rule_provider/manual-direct.yaml
    interval: 300

# ============================================================
# 主分流规则：严格按从上到下顺序匹配，第一条命中即停止。
# ============================================================
# 注意：RFC1918 三个私网段显式写出，不只依赖 GEOIP,private。
# 原因：实际运行日志已验证，某些环境下 192.168.100.1:53 未命中 GEOIP,private，
# 会落到最终 MATCH,美国。显式 CIDR 可确保局域网 DNS、PVE、NAS、WireGuard 等私网目标必定直连。
rules!:
  # 私有域名 / private GeoIP
  - GEOSITE,private,DIRECT
  - GEOIP,private,DIRECT,no-resolve

  # RFC1918 IPv4 私有地址：必须显式 DIRECT
  - IP-CIDR,10.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,172.16.0.0/12,DIRECT,no-resolve
  - IP-CIDR,192.168.0.0/16,DIRECT,no-resolve

  # 其他本地/特殊用途地址
  - IP-CIDR,127.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,169.254.0.0/16,DIRECT,no-resolve
  - IP-CIDR,224.0.0.0/4,DIRECT,no-resolve
  - IP-CIDR6,::1/128,DIRECT,no-resolve
  - IP-CIDR6,fc00::/7,DIRECT,no-resolve
  - IP-CIDR6,fe80::/10,DIRECT,no-resolve
  - IP-CIDR6,ff00::/8,DIRECT,no-resolve

  # 人工指定必须直连的域名
  - RULE-SET,Manual-Direct,DIRECT

  # 中国大陆域名 / IP
  - GEOSITE,cn,DIRECT
  - GEOIP,CN,DIRECT,no-resolve

  # 最终兜底：所有其他流量统一走美国
  - MATCH,美国
